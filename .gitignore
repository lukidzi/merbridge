# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
*.o

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/
.idea/

# The output of mbctl built binary
dist/

# the clangd compilation database
compile_commands.json

# the clangd generated code indexes for code navigation
.cache
.vscode
*.code-workspace

.DS_Store

build
bpf/.output
bpf/mb_bind
bpf/mb_connect
bpf/mb_get_sockopts
bpf/mb_recvmsg
bpf/mb_redir
bpf/mb_sendmsg
bpf/mb_sockops
bpf/mb_tc
bpf/mb_netns_cleanup
